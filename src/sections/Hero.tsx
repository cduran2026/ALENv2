import { Button } from '../components/Button'
import { SHOW_REVIEW_LABELS } from '../config'
import { ArrowRight } from 'lucide-react'
import { demoKpis } from '../data/dashboardDemo'

export function Hero() { const mini=[demoKpis[2],demoKpis[3],demoKpis[6]]; return <section id="inicio" className="hero-chapter"><div className="hero section-shell"><div className="hero-copy">{SHOW_REVIEW_LABELS&&<small>Contenido en revisión</small>}<p className="eyebrow">Consultoría + control de proyectos</p><h1>Controla proyectos complejos antes de que los problemas se vuelvan críticos.</h1><p>Gestión ambiental, permisos y herramientas inteligentes para anticipar riesgos, controlar hitos y tomar mejores decisiones.</p><div className="button-row"><Button href="#servicios">Conoce nuestras soluciones</Button><Button href="#control" secondary>Explorar ALEN Control</Button></div></div><div className="hero-visual" aria-label="Evolución conceptual de ALEN"><div className="expert-card"><span>ALEN</span><strong>Conocimiento experto</strong><p>Ambiental · Permisos · Territorio</p></div><div className="flow-line">Información gestionable <span>→</span></div><div className="control-card"><span>ALEN Control</span><strong>Decisiones más claras</strong><div className="mini-signals">{mini.map(item=><div className={`mini-signal tone-${item.tone}`} key={item.label}><span>{item.label}</span><strong>{item.value}</strong><small>{item.label==='Críticos'?'críticas':item.label==='Próximos hitos'?'próximos':'pendientes'}</small></div>)}</div></div></div></div><div className="challenge-strip section-shell"><div className="challenge-signals">{['Proyectos','Permisos','Hitos','Plazos','Responsables','Riesgos'].map(x=><span key={x}>{x}</span>)}</div><ArrowRight/><div><strong>Información gestionable</strong><p>Qué requiere atención y cuál es la próxima acción.</p></div></div></section> }
